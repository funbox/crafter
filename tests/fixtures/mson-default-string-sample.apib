# My API

Defines sample complex value for a string property member.

# GET /users

+ Response 200 (application/json)
    
    Неверный запрос #1
    
    + Attributes 
        + field1: `param_is_blank: (encrypted_text), param_is_not_present (encrypted_key)` (string, required) - Описание ошибки
        + field2: single (string, required) - другое
        + field3: `some, mix`, of, `strings`
        + field4: 1, 2, 3 (number, required)

