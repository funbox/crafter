# My API

# Data Structures

# User

+ name: `John` (string, required) - user name

# Admin

+ `access_level`: `all` (string, required)

# Get admin [GET /admin]

+ Response 200 (application/json)
    + Attributes(User)
        + Include Admin
