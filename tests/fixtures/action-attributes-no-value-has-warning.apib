# My API

# Create user [POST /user]

+ Response 200 (application/json)
    + Attributes
        + status (string)
        + age (number)
        + flag (boolean)
        + users (array)
        + obj (object)
        + kind (enum)
