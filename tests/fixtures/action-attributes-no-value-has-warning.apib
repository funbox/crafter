# My API

Defines attributes of a response message.
It produces warning 'Enum element "kind" should include members'.

# Create user [POST /user]

+ Response 200 (application/json)
    + Attributes
        + status (string)
        + age (number)
        + flag (boolean)
        + users (array)
        + obj (object)
        + kind (enum)
