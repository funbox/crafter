# My API

Defines block description for response attributes.
The separator (Items, Properties) follows the description.

# GET /foo

+ Response 200
    + Attributes(array)
        block description 1
        block description 2
        + Items
            + id (string)
            + name (string)

# GET /baz

+ Response 200
    + Attributes
        block description 1
        block description 2
        + Properties
            + id (string)
            + name: `John` (string)
