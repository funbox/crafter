# My API

# GET /foo

+ Response 200 (application/json)
    + Attributes(array[boolean])
        + Items
            + (object)
                + id (number)
                + name (string)
            + example 1 (string) - description 1
