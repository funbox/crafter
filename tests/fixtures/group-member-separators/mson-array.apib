# My API

Defines array items with Items keyword inside MSON structure.

# Data Structures

# Foo
+ bar (array[boolean], fixed-type)
    + Items
        + (object)
            + id (number)
            + name (string)
        + example 1 (string) - description 1
