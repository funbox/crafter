# My API

Defines array's item as a string element in email format.

# Users [/users]

## List [GET]

+ Response 200 (application/json)
    + Attributes
        + foo (array, required)
            + `test@gmail.com` (string, format="email")
