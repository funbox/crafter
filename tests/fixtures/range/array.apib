# My API

Defines array's item as a number element in a set of ranges

# Users [/users]

## List [GET]

+ Response 200 (application/json)
    + Attributes
        + foo (array, required)
            + 35 (number, minimum="18", maximum="100")
