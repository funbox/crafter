# My API

Defines property member "foo", which value must be a string and must not include more than 10 symbols

# Users [/users]

## List [GET]

+ Response 200 (application/json)
    + Attributes
        + foo (string, max-length="10")
