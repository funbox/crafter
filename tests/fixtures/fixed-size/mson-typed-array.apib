# My API

Defines property member "address" as an fixed-size array, which must contain exactly two elements,
both of them should be either string or number.

# Group Example
# Resource [/example]

## Action [GET]

+ Response 200 (application/json)
    + Attributes
        + address (array[string, number], min-length="2", max-length="5")
