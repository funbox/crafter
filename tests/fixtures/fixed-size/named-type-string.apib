# My API

Defines named type "CardNumber" as a string, which must include at least 11 symbols,
but no more than 16 symbols

# Data Structures

# CardNumber (string, min-length="11", max-length="16")

# Users [/users]

## List [GET]

+ Response 200 (application/json)
    + Attributes(CardNumber)
