# My API

Defines named type "FixedSizeFoo" as an array, which must contain exactly two elements of any type

# Data Structures

# FixedSizeFoo (array, min-length="3", max-length="3")

+ a1 - description 1

# GET /hello

+ Response 200 (application/json)
    + Attributes(FixedSizeFoo)
