# My API

Defines response attributes as a string with "min-length" type attribute, which indicates
that this string must include at least 3 symbols

# Users [/users]

## List [GET]

+ Response 200 (application/json)
    + Attributes (string, min-length="3")
