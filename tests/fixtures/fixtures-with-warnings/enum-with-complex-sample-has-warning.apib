# My API

Defines a data structure with an enum of objects and a sample section in it.
Emits the next warning: "Enum must not use non-primitive or named types as a sub-type".
Sample is parsed as the literal value "(object)".

# Data Structures

## ReportType

+ `report_type` (enum[object])
    + Sample
      + (object)
    + push
    + sms

# GET /foo

+ Response 200 (application/json)
    + Attributes(ReportType)
