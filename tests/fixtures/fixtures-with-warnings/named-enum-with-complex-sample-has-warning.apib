# My API

Defines a data structure with an enum of objects and a default section in it.
Emits the next warning: "Enum must not use non-primitive or named types as a sub-type".
Sample is parsed as the literal value "(object)".

# Data Structures

## ReportType (enum[object])

### Members

+ push
+ sms

### Sample

+ (object)
