# My API

Defines enum-type properties without members.
It produces warnings 'Enum element "state" should include members.', 'Enum element "kind" should include members.',
'Enum element "process" should include members.'


# Data Structures

## Enum1
+ state (enum) - enum with no members

## Enum2
+ kind (enum) - enum with no members, but with default and sample values
    + Default: `sms`
    + Sample: `sms`

## Enum3
+ process (enum) - enum with "Members" section separator but with not actual members
  + Members
