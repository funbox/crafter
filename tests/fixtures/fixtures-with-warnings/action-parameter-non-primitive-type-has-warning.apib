# My API

Defines a non-primitive data type for a URI parameter.
It produces warning "Using non-primitive data types for parameters violates API Blueprint Spec".

# List users [GET /users/{?id}]

+ Parameters
    + id: `5,10` (array[number], required) - user IDs

+ Response 200
