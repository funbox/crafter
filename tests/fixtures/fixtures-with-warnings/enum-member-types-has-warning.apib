# My API

Only "string", "boolean" and "number" are valid types for enum members, other types produce warnings.

# Data Structures

# A (object)
- id: 1

# Test enum (enum)
- string is the default type if type is omitted
- invalid type (A)
- true (boolean)
- false (boolean)
- 13 (number)
- invalid type 2 (array[A])
- valid 2 (string)
- invalid type 3 (object)
