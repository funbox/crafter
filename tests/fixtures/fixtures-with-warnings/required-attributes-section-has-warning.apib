# My API

Type attribute "required" has no meaning when used in "+ Attributes" signature.
This fixture produces a warning: 'Type attribute "required" is not allowed for a value of type "object"'

## GET /foo

+ Response 200 (application/json)
    + Attributes (object, required)
        + key1: `value1` (string)
        + key2: `value2` (string)
