# My API

Defines property member "result" as an array of numbers, which includes untyped nested elements.
These elements should inherit type "number" and the last element value should be replaced with "0".
It produces warning:
'Invalid value "test" for "number" type'

# GET /foo

+ Response 200 (application/json)
    + Attributes
      + result (array[number])
          + 1
          + 2
          + test
