# My API

Defines a data structure with an enum of objects and a default section in it.
Emits the warning "Default values of enum of non-primitive types are not supported".

# Data Structures

## ReportType (enum[object])

### Members

+ push
+ sms

### Default

+ (object)
