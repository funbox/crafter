# My API

Defines wrong-indented request header.
It produces the next warning:
"Headers" is expected to be a pre-formatted code block, every of its line indented by exactly 12 spaces or 3 tabs.

# Group Some Group

## Some Subgroup [/api/v1/foo]

### Some Req [PUT /api/v1/foo/bar]

+ Request (application/gzip)

    + Headers

        Foo: "Bar"

+ Response 200 (application/json)
