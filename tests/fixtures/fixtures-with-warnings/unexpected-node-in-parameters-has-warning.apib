# My API

Defines parameters.
Uses some unrecognizable list item section.
It produces warning 'Ignoring unrecognized block "+ c ("'.

# GET /foo{a,b}

+ Parameters
  + a (string, required)
  + b (string, optional)
  + c (

+ Response 200
