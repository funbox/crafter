# My API

Defines sample values for various mson members. Value types of members and their sample values must be the same, but they are not.
It produces warning 'Invalid value format "100" for type "boolean"', 'Invalid value format "true" for type "number"' and so on.

## Data Structures

### Profile

+ sms_notices (boolean, required)
    + Sample: 100
+ balance (number)
    + Sample
        + true
+ username (string, required)
    + Sample: false
+ reportId (enum[number])
    + Sample: sms
    + 101
    + 202
+ userIds (array[number])
    + Sample: user_1, user_2
+ zoneIds (array[number])
    + Sample
        + zone_1
        + zone_2

# GET /foo

+ Response 200 (application/json)
    + Attributes(Profile)
