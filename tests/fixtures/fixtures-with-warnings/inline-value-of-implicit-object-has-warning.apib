# My API

Defines MSON object "foo" with some nested elements and an inline-defined value.
Produces warning '"object" with value definition. You should use type definition without value, e.g., "+ key (object)"'

# Data Structures

## Obj

+ a
+ b

# GET /foo

+ Response 200 (application/json)
    + Attributes
        + foo: hello
            + test (string)
