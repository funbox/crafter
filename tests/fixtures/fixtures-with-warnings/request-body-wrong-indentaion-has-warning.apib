# My API

Defines wrong-indented request body.
It produces the next warning:
"Body" is expected to be a pre-formatted code block, every of its line indented by exactly 12 spaces or 3 tabs.

# List users [GET /users]

+ Request (application/json)
    + Body

        {
            "hello": "world"
        }

+ Response 200
