# My API

Defines URI parameter as required but describes its default value.
It produces warning 'Specifying parameter name as required supersedes its default value, declare the parameter as 'optional' to specify its default value.'.


# List users [GET /users/{name}]

+ Parameters
    + name: `John` (required) - user name
        + Default: `Ivan`

+ Response 200
