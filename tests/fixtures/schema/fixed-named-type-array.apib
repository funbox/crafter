# My API

Defines named type as an array using 'fixed' type attribute which indicates that the structure
includes only the specified values and types of its nested member types.

# Data Structures

# FixedFoo (array, fixed)

+ a1 - description 1

# SimpleFoo (array)

+ a2 - description 2

# GET /hello

+ Response 200 (application/json)
    + Attributes(FixedFoo)

+ Response 200 (application/json)
    + Attributes(SimpleFoo, fixed)
