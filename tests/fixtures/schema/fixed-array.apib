# My API

# GET /foo

+ Response 200 (application/json)
    + Attributes
        + tags (array, fixed)
            + hello (string)
            + 42 (number)
            + (object)
                + a: hello
                + b: world
