# My API

Defines attribute of array type using 'fixed' type attribute which indicates that the structure
includes only the specified values and types of its nested member types.

# GET /foo

+ Response 200 (application/json)
    + Attributes
        + tags (array, fixed)
            + hello (string)
            + 42 (number, sample)
            + (object)
                + a: hello
                + b: world
