# My API

Defines attribute as an object with the "fixed-type" type attribute, which contains property "c" defined as another object.
The example given shows that the "fixed-type" type attribute must not propagate to nested members.

# GET /foo

+ Response 200 (application/json)
    + Attributes
        + o (object, fixed-type)
            + c (object)
                + test: `foo` (string)
