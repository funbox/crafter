# My API

Defines named type as an object with strongly fixed type structure using 'fixed-type' type attribute.
Further, such an object type structure must not contain any other properties.

# Data Structures

# FixedTypeFoo (fixed-type)

+ a1: `b1` (string) - description 1

# SimpleFoo

+ a2: `b2` (string) - description 2

# GET /hello

+ Response 200 (application/json)
    + Attributes(FixedTypeFoo)

+ Response 200 (application/json)
    + Attributes(SimpleFoo, fixed-type)
