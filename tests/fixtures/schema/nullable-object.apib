# My API

Defines attribute as an object or null using 'nullable' type attribute and overrides
'c' property as optional. Implies that 'o' can be either an object that contains 'a' and 'b' properties and may contain 'c', or 'null'.

# GET /foo

+ Response 200 (application/json)
    + Attributes
        + o (object, nullable)
            + a: hello
            + b: world
            + c: bar (optional)
