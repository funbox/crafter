# My API

Defines attribute as an object with strongly fixed type structure and value by using 'fixed' type attribute and overrides
'c' property as optional. Implies an object that must contain 'a' and 'b' properties and may contain 'c'.

# GET /foo

+ Response 200 (application/json)
    + Attributes
        + o (object, fixed)
            + a: hello
            + b: world
            + c: bar (string, sample, optional)
