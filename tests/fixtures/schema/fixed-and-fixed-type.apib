# My API

# Data Structures

## A
+ foobar (object)
+ One Of
    + Properties
        + foo_str (string)
        + foo_obj (object)
    + Properties
        + bar_str (string)
        + bar_obj (string)

# Request [GET /first]

+ Response 200 (application/json)
    + Attributes (A, required, fixed-type)

+ Response 300 (application/json)
    + Attributes
        + result (A, required, fixed)
