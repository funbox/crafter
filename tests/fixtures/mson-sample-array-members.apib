# My API

Defines sample values for an array members with 'sample' type attribute.

# Data Structures

# User

+ tags (array)
  + user (string, sample)
  + admin (string, sample)
+ kind (enum)
  + track (string, sample)
  + time (string, sample)
