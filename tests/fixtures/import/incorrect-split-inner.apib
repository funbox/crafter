## Client groups [/client_groups]

### Get client groups [GET /client_groups{?sort_field}]

+ Parameters
    + sort_field: `name` (enum[string], optional)
        + Default: `name`
        + Members
            + `name`
            + `list_type`
            + `numbers_count`
            + `created_at`

+ Response 200 (application/json)
