# My API

This fixture imports a data structures section, which contains inheritance from an unknown type.
It produces error 'Type "Unknown" not found' with the reference to the imported file.

# Import import-with-unknown-type-inner.apib

# GET /foo

+ Response 200 (application/json)
    + Attributes (A)
