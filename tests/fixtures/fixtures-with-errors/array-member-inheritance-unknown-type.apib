# My API

Defines an array member, which inherits from an unknown type. It produces error 'Type "Unknown" not found'.

# GET /foo

+ Response 200 (application/json)
    + Attributes (object)
        + myArr (array)
            + test (Unknown)
