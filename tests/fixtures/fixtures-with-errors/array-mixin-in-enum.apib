# My API

Defines an array mixin in enum structure.
Produces the error "Mixin base type should be the same as parent base type: enums should contain enum mixins.".

# Data Structures

## Cities (array)

+ Moscow
+ London

## PostCode (enum)

+ N1

# Get cities [GET /cities]

+ Response 200 (application/json)
    + Attributes(PostCode)
        + Include Cities
