# My API

Defines an array mixin in object structure.
Produces the error "Mixin base type should be the same as parent base type: objects should contain object mixins.".

# Data Structures

## Cities (array)

+ Moscow
+ London

## User (object)

+ name

# Get cities [GET /cities]

+ Response 200 (application/json)
    + Attributes(User)
        + Include Cities
