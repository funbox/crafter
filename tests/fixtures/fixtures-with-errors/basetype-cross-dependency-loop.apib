# My API

Defines data structures which are dependent from each other.
It produces error 'Dependencies loop: Admin - User - Admin'.

# Data Structures

# User (Admin)

+ name: `John` (string, required) - user name

# Admin (User)

+ `access_level`: `all` (string, required)

# Get admin [GET /admin]

+ Response 200
    + Attributes(Admin)
