# My API

Includes undefined mixin in response attributes' member.
It produces error 'Mixin "UnknownType" is not defined in the document'.

# Data Structures

# User

+ name: `John` (string, required) - user name

# Get admin [GET /admin]

+ Response 200
    + Attributes(User)
        + customAttribute
          + Include UnknownType
