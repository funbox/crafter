# My API

Defines a response with attributes, which inherit from object with nested types.
It produces error 'Invalid type object[string]. Nested types should be present only for types which are sub typed
from either array or enum structure type'.

# GET /foo

+ Response 200 (application/json)
    + Attributes (object[string])
