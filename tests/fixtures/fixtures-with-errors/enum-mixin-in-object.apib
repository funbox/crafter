# My API

Defines an enum mixin in object structure.
Produces the error "Mixin base type should be the same as parent base type: objects should contain object mixins.".

# Data Structures

## User

+ name

## PostCode (enum)

+ N1

# Get user [GET /user]

+ Response 200 (application/json)
    + Attributes(User)
        + Include PostCode
