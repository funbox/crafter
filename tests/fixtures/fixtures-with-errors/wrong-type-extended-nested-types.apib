# My API

Extends named type of type object with nested type.
It produces error 'Invalid type SimpleType[number]. Nested types should be present only for types which are sub typed
from either array or enum structure type'.

# Data Structures

## SimpleType

+ prop

## MixedType (SimpleType[number])

# Get options [GET /mixed]

+ Response 200 (application/json)
    + Attributes(MixedType)
