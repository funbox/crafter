# My API

Includes undefined mixin in response attributes.
It produces error 'Mixin "UnknownType" is not defined in the document'.

# Data Structures

# User

+ name: `John` (string, required) - user name

# Get admin [GET /admin]

+ Response 200
    + Attributes(User)
        + Include UnknownType
