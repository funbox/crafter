# My API

Defines named type using wrong format with inline description.
It produces error 'Invalid NamedType definition. Expected format: "# Type Name (Type Attributes)"'.

# Data Structures

# User

+ name: `John` (string, required) - user name

# Admin (User) - unacceptable description

+ `access_level`: `all` (string, required)

# Get admin [GET /admin]

+ Response 200
    + Attributes(Admin)
