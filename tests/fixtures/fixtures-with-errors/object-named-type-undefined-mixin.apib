# My API

Includes undefined mixin in named type.
It produces error 'Mixin "UnknownType" is not defined in the document'.

# Data Structures

# User

+ name: `John` (string, required) - user name

# Admin

+ Include UnknownType
+ `access_level`: `all` (string, required)

# Get admin [GET /admin]

+ Response 200
    + Attributes(Admin)
