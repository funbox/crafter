# My API

Defines object-type property member type using only default values.

# Data Structures

# User

+ name: `John` (string, required) - user name
+ permissions (object)
    + Default
      + posts: true
      + comments: true

# Get user [GET /user]

+ Response 200 (application/json)
    + Attributes(User)
