# API name

Defines resource prototypes and uses them together - both prototypes for the same resource.

# Resource Prototypes

## NotFound

+ Response 404

## InternalError

+ Response 500

# Group Users (NotFound)

## GET /users (NotFound, InternalError)

+ Response 200
