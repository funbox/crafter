# API name

Defines an attempt to use an empty string as a prototype name.
Only 'NotFound' prototype should be accounted in result.

# Resource Prototypes

## NotFound

+ Response 404

# Group Users

## GET /users (NotFound, )

+ Response 200
