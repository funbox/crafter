# My API

Defines array's item with a regular expression pattern.

# Users [/users]

## List [GET]

+ Response 200 (application/json)
    + Attributes
        + foo (array, required)
            + `my example` (string, pattern="^\\S+$")
