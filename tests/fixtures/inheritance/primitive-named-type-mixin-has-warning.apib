# My API

Defines data structure inherited from primitive type (string) and uses it as a mixin.
It produces warning 'Mixin may not include a type of a primitive sub-type'.

# Data Structures

## Foo (string)

## Bar

+ Include Baz

## Baz (Foo)

# GET /foo

+ Response 200 (application/json)
    + Attributes
        + o1 (object)
            + Include Foo
        + a1 (array)
            + Include Foo
        + e1 (enum)
            + test
            + Include Foo
