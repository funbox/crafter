# My API

Defines Attributes as an array, which includes members from other array named 'Address'.
Resulting array should include both 'Karlin' and 'Prague' as members.

# Group Example

# GET /

+ Response 200 (application/json)
    + Attributes (array)
        + Karlin
        + Include Address

# Data Structures

## Address (array)
+ Prague
