# My API

Defines object-type User data structure and uses it as a mixin.

# Data Structures

# User

+ name: `John` (string, required) - user name

# Admin

+ Include User
+ `access_level`: `all` (string, required)

# Get admin [GET /admin]

+ Response 200 (application/json)
    + Attributes(Admin)
