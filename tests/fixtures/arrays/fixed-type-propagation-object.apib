# My API

Defines property member type as an array with the "fixed-type" type attribute, which contains elements of named type "A".
The example given shows that the "fixed-type" type attribute must not propagate to nested members.

# Data Structures

## A
+ foo (string, required)

## Получить местоположения [GET /users]

+ Response 200 (application/json)
    + Attributes
        + result (array[A])
