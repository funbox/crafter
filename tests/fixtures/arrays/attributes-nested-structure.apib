# My API

Defines attributes as an array structure using in-place array member definition.

# GET /foo

+ Response 200 (application/json)
    + Attributes(array[boolean])
        + (object)
            + id (number)
            + name (string)
        + example 1 (string) - description 1
