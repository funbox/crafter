# My API

Defines an untyped array (no nested types specified) with the 'fixed-type' type attribute.
Produces warning 'fixed-type keyword is redundant'.

# Get user [GET /invites_list]

+ Response 200 (application/json)
    + Attributes
        + folks (array, fixed-type)
