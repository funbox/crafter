# My API

Defines array-type named type inherited from another array-type named type.

# Data Structures

## ArrayType (array)
+ example (string)

## ArrayTypeExtended (ArrayType)
+ (object)
   + id (number)
   + name (string)

# Get options [GET /options]

+ Response 200 (application/json)
    + Attributes(ArrayTypeExtended)
