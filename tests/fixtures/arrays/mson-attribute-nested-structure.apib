# My API

Defines property member type as an array structure using in-place object definition.

# Data Structures

# Foo
+ bar (array[boolean], fixed-type)
    + (object)
        + id (number)
        + name (string)
    + example 1 (string) - description 1
