# My API

Defines arrays' members values using a list-defined Default keyword.

# Data Structures

## User

+ a1 (array)
    + Default: foo, bar
+ a2 (array[number])
    + Default
        + 100
        + 200

# GET /foo

+ Response 200 (application/json)
    + Attributes(User)
