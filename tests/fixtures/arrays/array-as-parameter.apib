# My API

Defines a URI parameter of array type using nested list for array members without an appropriate keyword (Items).
It will be parsed as a description.

# Group Заявки

## Список заявок [/applications]

## Запрос списка заявок [GET /applications.json{?filter%5Bstates%5D*}]

+ Parameters
    + filter%5Bstates%5D (string, optional) - фильтр по статусу
        + delayed_execution - отложенное выполнение
        + delayed_partial_execution - отложенное частичное выполнение
        + completed - выполнена

+ Response 200 (application/json)
