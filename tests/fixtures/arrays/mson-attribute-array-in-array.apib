# My API

Defines property member type as an array using in-place array member definition.

# Data Structures

# Foo
+ bar (array[boolean], fixed-type)
    + example 1 (string) - description 1
    + (array, fixed-type)
        + (number)

# Create user [POST /user]

+ Response 200 (application/json)
    + Attributes(Foo)

