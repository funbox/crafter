# My API

Defines a typed array (nested elements should have 'number' type) with the 'fixed-type' type attribute.
Produces warning 'fixed-type keyword is redundant'.

# Get user [GET /invites_list]

+ Response 200 (application/json)
    + Attributes
        + folks (array[number], fixed-type)
