# My API

Defines sample values for a property member types with 'sample' type attribute.

# GET /foo

+ Response 200 (application/json)
    + Attributes
        + a1: foo, bar, baz (array, sample)
        + a2: first, `hello, world`, second (array, sample)
        + a2: 1.1, 3.5, 4 (array[number], sample)
        + a3: true, false, foo (array[boolean], sample)
