# My API

Defines property member "result" as an array of Note named type elements.
This array includes nested elements, which should not inherit type "Note" and must be defined as strings

# Data Structures

# Note

+ text (string)

# GET /foo

+ Response 200 (application/json)
    + Attributes
      + result (array[Note])
          + 0
          + 1
          + 2
