# My API

Defines named types:

1) "SimpleType" as an array with nested member type list;
2) "MixedType", which inherits from "SimpleType" and extends its nested type list.

Expected result:

Both string and number elements can be included in attributes body.
Generated JSON schema must define response attributes as an array with items of type "string" or "number".

# Data Structures

## SimpleType (array[string])

## MixedType (SimpleType[number])

# Get options [GET /mixed]

+ Response 200 (application/json)
    + Attributes(MixedType)
