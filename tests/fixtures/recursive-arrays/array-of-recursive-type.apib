# My API

Defines attributes as an array of recursive named type.

# Data Structures

## Comment

+ text
+ children (array[Comment])

# GET /comments

+ Response 200 (application/json)
    + Attributes (array[Comment])
