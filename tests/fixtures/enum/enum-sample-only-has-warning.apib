# My API

Defines enum sample value without specifying its members.
It produces warning 'Enum element "kind" should include members'.

## Report templates [/report_templates]

### Create report template [POST /report_templates]

+ Response 200 (application/json)
    + Attributes
        + kind (enum)
            + Sample: sms
