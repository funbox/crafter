# My API

Defines value of wrong type.
It produces warning 'Invalid value format "sms" for enum type 'number''.

# Data Structures

## Enum1
+ id (enum[number]) - typed enum, with a member with non-specified type
    + 53 - описание 1
    + 12 - описание 2
    + 72 - описание 3
    + sms - описание 4

## Enum2
+ kind (enum) - non-typed enum, by default string, with type overriding on last member
    + code - описание 1
    + sms - описание 2
    + mail - описание 3
    + 33 (number) - описание 4
