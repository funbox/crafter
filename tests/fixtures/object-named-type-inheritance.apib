# My API

Defines object-type Admin named type inherited from object-type User named type.

# Data Structures

# User

+ name: `John` (string, required) - user name

# Admin (User)

+ `access_level`: `all` (string, required)

# Get admin [GET /admin]

+ Response 200 (application/json)
    + Attributes(Admin)
