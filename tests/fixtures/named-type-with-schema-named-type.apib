# My API

Defines named type property by using schema named type.

# Data Structures

# NamedType

+ foo (SchemaNamedType)
+ bar (array[SchemaNamedType])

# Schema Structures

# SchemaNamedType

+ Body

      {
          "test": "Hello"
      }

+ Schema

      {
          "$schema": "http://json-schema.org/draft-04/schema#",
          "type": "object",
          "properties": {
              "test": {
                  "type": "string"
              }
          }
      }

# Get test [GET /test]

+ Response 200 (application/json)
  + attributes(NamedType)
