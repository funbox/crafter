# My API

Defines object property by using schema named type and One Of construction.

# Schema Structures

# SchemaNamedType

+ Body

      {
          "test": "Hello"
      }

+ Schema

      {
          "$schema": "http://json-schema.org/draft-04/schema#",
          "type": "object",
          "properties": {
              "test": {
                  "type": "string"
              }
          }
      }

# Get test [GET /test]

+ Response 200 (application/json)
  + attributes
    + One of
      + foo (SchemaNamedType)
      + bar (number)
