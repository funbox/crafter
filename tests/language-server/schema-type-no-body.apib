# My API

In Language Server schema type without body should not raise an error

# Schema Structures

## User
+ Schema

        {
          "type": "object"
        }

# GET /user

+ Response 200 (application/json)
    + Attributes (User)
